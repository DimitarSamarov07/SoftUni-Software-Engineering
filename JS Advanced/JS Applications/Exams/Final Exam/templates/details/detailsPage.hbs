{{> header}}
<div class="container">
    <div class="row bg-light text-dark">
        <h1>Movie title: Black Widow</h1>

        <div class="col-md-8">
            <img class="img-thumbnail" src="{{imageUrl}}" alt="Movie">
        </div>
        <div class="col-md-4 text-center">
            <h3 class="my-3 ">Movie Description</h3>
            <p>{{description}}</p>
            {{#if isAuthor}}
                <a class="btn btn-danger" href="#/delete/{{objectId}}">Delete</a>
                <a class="btn btn-warning" href="#/edit/{{objectId}}">Edit</a>
            {{else}}
                {{#unless hasUserLiked}}
                    <a class="btn btn-primary" href="#/like/{{objectId}}">Like</a>
                {{else}}
                    <span class="enrolled-span">Liked {{likesCount}}</span>
                {{/unless}}
            {{/if}}
        </div>
    </div>
</div>
{{> footer}}